{
  plugins.mini = {
    enable = true;
    modules = {
      operators = {
        evaluate = {
          prefix = "<leader>=";
        };
        replace = {};
        sort = {};
      };
    };
  };
}
